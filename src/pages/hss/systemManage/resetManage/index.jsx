// libraries
// components
import Layout from '@components/layouts';
function ResetManage() {
  // JSX
  return (
    <>
    </>
  );
}

ResetManage.getLayout = function getLayout(page) {
  return <Layout>{page}</Layout>;
};

export default ResetManage;
