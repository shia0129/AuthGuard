import baseApi from '@api/common/baseApi';

const serverFileTransHistoryApi = {
  ...baseApi,

  getServerFileTransHistoryList: (parameters) => {
    // 조회api
  },
};

export default serverFileTransHistoryApi;
