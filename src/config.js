export const drawerWidth = 260;

export const DEFAULT_PATH = '/home/dashboard';

// ==============================|| THEME CONFIG  ||============================== //

const config = {
  fontFamily: `'Public Sans', sans-serif`,
  i18n: 'ko',
  miniDrawer: false,
  container: false,
  mode: 'light',
  menuMode: 'dark',
  tableMode: 'vertical',
  presetColor: 'default',
  fullSizeFlag: true,
  notificationTime: 600000,
};

export default config;
